# API Gateway URL — all service traffic routes through here
# In production this is the Envoy Gateway external IP/hostname
# In local dev use the port-forwarded gateway port
VITE_GATEWAY_URL=http://localhost:5000
